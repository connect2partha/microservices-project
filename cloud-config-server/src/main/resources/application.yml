server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/connect2partha/config-server/
          clone-on-start: true

#Below are added in GitHub config-server          
#eureka:
#  instance:
#    hostname: localhost
#  client:
#    register-with-eureka: true
#    fetch-registry: true
#    service-url:
#      defaultzone: http://localhost:8761/eureka/
        